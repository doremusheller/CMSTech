(() => {
  const meta = name => document.querySelector(`meta[name="${name}"]`)?.content?.trim();
  const clientId = meta("cms-google-oauth-client-id");
  const siteUrl = meta("cms-search-console-site");
  const propertyName = meta("cms-google-analytics-property") || "CMS Tech Website";
  const host = document.querySelector(".foot");
  if (!host) return;

  const style = document.createElement("style");
  style.textContent = `.google-intelligence{grid-column:1/-1;display:grid;gap:14px;border-color:rgba(97,169,255,.38);background:linear-gradient(135deg,rgba(22,45,82,.28),rgba(23,10,6,.88))}.google-intelligence .head{margin:0}.google-connect{border:1px solid #61a9ff;border-radius:9px;background:rgba(42,96,167,.22);color:#d7e8ff;padding:9px 12px;font-weight:800;cursor:pointer}.google-connect:disabled{opacity:.5}.google-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px}.google-stat{min-height:78px;padding:12px 14px;border:1px solid rgba(255,255,255,.09);border-radius:11px;background:rgba(0,0,0,.2)}.google-stat span{display:block;color:#a99180;font-size:8px;font-weight:800;letter-spacing:.14em;text-transform:uppercase}.google-stat strong{display:block;margin-top:7px;color:#8ab9ff;font:500 25px Georgia,serif}.google-stat small{color:#947b6a;font-size:9px}.google-details{display:grid;grid-template-columns:1.2fr .8fr;gap:12px}.google-sub{padding:13px;border:1px solid rgba(255,255,255,.08);border-radius:11px;background:rgba(0,0,0,.16)}.google-sub h3{margin:0 0 10px;color:#fff0dc;font:500 16px Georgia,serif}.google-list{display:grid}.google-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;padding:8px 0;border-top:1px solid rgba(255,255,255,.07);color:#c9b7a9;font-size:10px}.google-row b{color:#ffc247}.google-trend{height:120px;width:100%}.google-line{fill:none;stroke:#61a9ff;stroke-width:3;stroke-linecap:round;stroke-linejoin:round}.google-area{fill:rgba(97,169,255,.09)}.google-status{margin:0;color:#8f7a6d;font-size:10px}.google-status.error{color:#ff9b6b}.google-status.live{color:#75dcca}@media(max-width:900px){.google-grid{grid-template-columns:repeat(2,1fr)}.google-details{grid-template-columns:1fr}}@media(max-width:560px){.google-grid{grid-template-columns:1fr}}`;
  document.head.appendChild(style);
  const panel = document.createElement("section");
  panel.className = "block google-intelligence";
  panel.innerHTML = `<div class="head"><div><p class="kicker">Google · Public intelligence</p><h2 class="title">Website &amp; Search Visibility</h2></div><button class="google-connect" id="googleConnect">Connect Google Analytics</button></div><div class="google-grid"><div class="google-stat"><span>Active now</span><strong id="gaActiveNow">—</strong><small>Realtime visitors</small></div><div class="google-stat"><span>30-day visitors</span><strong id="gaUsers">—</strong><small>Active users</small></div><div class="google-stat"><span>Page views</span><strong id="gaViews">—</strong><small>Last 30 days</small></div><div class="google-stat"><span>Search impressions</span><strong id="gscImpressions">—</strong><small>Last 28 days</small></div></div><div class="google-details"><div class="google-sub"><h3>Audience trend</h3><svg class="google-trend" id="gaTrend" viewBox="0 0 600 120" preserveAspectRatio="none"></svg></div><div class="google-sub"><h3>Top pages</h3><div class="google-list" id="gaPages"><div class="google-row"><span>Connect Google to load data</span><b>—</b></div></div></div><div class="google-sub"><h3>Traffic sources</h3><div class="google-list" id="gaSources"><div class="google-row"><span>Connect Google to load data</span><b>—</b></div></div></div><div class="google-sub"><h3>Search queries</h3><div class="google-list" id="gscQueries"><div class="google-row"><span>Connect Google to load data</span><b>—</b></div></div></div></div><p class="google-status" id="googleStatus">Your Microsoft sign-in remains the console gate. Google authorization is read-only and one-time for this browser session.</p>`;
  host.before(panel);
  const $ = id => document.getElementById(id);
  const fmt = value => new Intl.NumberFormat("en-US", { maximumFractionDigits: 0 }).format(Number(value || 0));
  const esc = value => String(value ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  let tokenClient, accessToken, propertyId;
  const tokenScope = "https://www.googleapis.com/auth/analytics.readonly https://www.googleapis.com/auth/webmasters.readonly";
  function loadGis() { return new Promise((resolve, reject) => { if (window.google?.accounts?.oauth2) return resolve(); const script=document.createElement("script"); script.src="https://accounts.google.com/gsi/client"; script.onload=resolve; script.onerror=reject; document.head.appendChild(script); }); }
  async function api(url, body) { const response=await fetch(url,{method:"POST",headers:{Authorization:`Bearer ${accessToken}`,"Content-Type":"application/json"},body:JSON.stringify(body)}); if(!response.ok) throw new Error(`Google API returned ${response.status}`); return response.json(); }
  async function discoverProperty() { const response=await fetch("https://analyticsadmin.googleapis.com/v1beta/accountSummaries",{headers:{Authorization:`Bearer ${accessToken}`}}); if(!response.ok) throw new Error("Could not list Analytics properties"); const data=await response.json(); for(const account of data.accountSummaries||[]) { const match=(account.propertySummaries||[]).find(p=>p.displayName===propertyName); if(match) return match.property?.replace(/^properties\//,""); } throw new Error(`Could not find the Analytics property “${propertyName}”`); }
  const rows = report => report.rows || [];
  function list(id, data, label, value, empty) { $(id).innerHTML=data.length?data.slice(0,6).map(row=>`<div class="google-row"><span>${esc(label(row))}</span><b>${esc(value(row))}</b></div>`).join(""):`<div class="google-row"><span>${esc(empty)}</span><b>—</b></div>`; }
  function drawTrend(data) { const values=data.map(row=>Number(row.metricValues?.[0]?.value||0)); if(!values.length)return; const peak=Math.max(1,...values), points=values.map((v,i)=>`${values.length===1?300:i*600/(values.length-1)},${108-v/peak*94}`); $("gaTrend").innerHTML=`<path class="google-area" d="M0 108 L${points.join(" L")} L600 108 Z"/><polyline class="google-line" points="${points.join(" ")}"/>`; }
  async function loadData() {
    $("googleStatus").textContent="Loading Google data…"; $("googleConnect").disabled=true;
    try {
      propertyId=propertyId||await discoverProperty(); const base=`https://analyticsdata.googleapis.com/v1beta/properties/${propertyId}`;
      const batch=await api(`${base}:batchRunReports`,{requests:[{dateRanges:[{startDate:"30daysAgo",endDate:"today"}],metrics:[{name:"activeUsers"},{name:"screenPageViews"}]},{dateRanges:[{startDate:"30daysAgo",endDate:"today"}],dimensions:[{name:"date"}],metrics:[{name:"activeUsers"}],orderBys:[{dimension:{dimensionName:"date"}}]},{dateRanges:[{startDate:"30daysAgo",endDate:"today"}],dimensions:[{name:"pagePath"},{name:"pageTitle"}],metrics:[{name:"screenPageViews"}],orderBys:[{metric:{metricName:"screenPageViews"},desc:true}],limit:"6"},{dateRanges:[{startDate:"30daysAgo",endDate:"today"}],dimensions:[{name:"sessionDefaultChannelGroup"}],metrics:[{name:"sessions"}],orderBys:[{metric:{metricName:"sessions"},desc:true}],limit:"6"}]});
      const reports=batch.reports||[], summary=reports[0]||{}, trend=reports[1]||{}, pages=reports[2]||{}, sources=reports[3]||{}; const summaryValues=summary.rows?.[0]?.metricValues||[]; $("gaUsers").textContent=fmt(summaryValues[0]?.value); $("gaViews").textContent=fmt(summaryValues[1]?.value); drawTrend(trend.rows||[]);
      const realtime=await api(`${base}:runRealtimeReport`,{metrics:[{name:"activeUsers"}]}); $("gaActiveNow").textContent=fmt(realtime.rows?.[0]?.metricValues?.[0]?.value);
      list("gaPages",rows(pages),r=>r.dimensionValues?.[1]?.value||r.dimensionValues?.[0]?.value,r=>`${fmt(r.metricValues?.[0]?.value)} views`,"No page data yet"); list("gaSources",rows(sources),r=>r.dimensionValues?.[0]?.value,r=>`${fmt(r.metricValues?.[0]?.value)} sessions`,"No source data yet");
      const end=new Date(); end.setUTCDate(end.getUTCDate()-3); const start=new Date(end); start.setUTCDate(start.getUTCDate()-27); const date=d=>d.toISOString().slice(0,10); const searchBase=`https://searchconsole.googleapis.com/webmasters/v3/sites/${encodeURIComponent(siteUrl)}/searchAnalytics/query`; const search=await api(searchBase,{startDate:date(start),endDate:date(end),dimensions:["query"],rowLimit:10}); const searchRows=search.rows||[]; const impressions=searchRows.reduce((sum,r)=>sum+Number(r.impressions||0),0); $("gscImpressions").textContent=fmt(impressions); list("gscQueries",searchRows,r=>r.keys?.[0],r=>`${fmt(r.impressions)} impressions`,"No search-query data yet"); $("googleStatus").textContent="Connected with read-only Google access for this browser session."; $("googleStatus").className="google-status live"; $("googleConnect").textContent="Refresh Google data";
    } catch(error) { console.error(error); $("googleStatus").textContent=error.message||"Google data is temporarily unavailable"; $("googleStatus").className="google-status error"; $("googleConnect").textContent="Connect Google Analytics"; } finally { $("googleConnect").disabled=false; }
  }
  async function connect() { if(!clientId||clientId.includes("REPLACE")){ $("googleStatus").textContent="Google OAuth client ID is not configured yet."; $("googleStatus").className="google-status error"; return; } await loadGis(); tokenClient=tokenClient||google.accounts.oauth2.initTokenClient({client_id:clientId,scope:tokenScope,callback:response=>{accessToken=response.access_token; loadData();}}); tokenClient.requestAccessToken({prompt:accessToken?"":"consent"}); }
  $("googleConnect").addEventListener("click",connect);
})();
